import joinProperties from "@/utils/join-properties";
import classes from "@/utils/classes-definition";

const defineSearch = {
  // a: joinProperties(p1),
  // b: joinProperties(p2),
  // c: joinProperties(p3),
};

const defSearch = "Que cagado";
// const defNo = joinProperties(defineSearch);

export default defSearch;
