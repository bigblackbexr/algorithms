import no from "@/components/algorithm.vue";

// All routes below here

const noCategory = [
  {
    path: '/no',
    name: 'no',
    component: no,
    props: true
  },

];

export default noCategory;